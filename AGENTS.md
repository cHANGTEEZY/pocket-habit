# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Design Context

This project has captured design context. Read these before UI work:

- `PRODUCT.md` — register (product), platform (adaptive: iOS + Android + web), users, purpose, brand personality, anti-references, and design principles.
- `DESIGN.md` — visual system: colors (Signal Blue accent, OKLCH), typography, elevation, and components, extracted from the HeroUI Native theme.

North Star: **Calm Focus** — the task is the hero, one accent used sparingly, premium through restraint. Do not ship a generic AI-slop template look.
