diff --git a/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts b/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts
index 1ff17b7..7cb6801 100644
--- a/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts
+++ b/node_modules/expo-glass-tabs/build/glass-tab-bar.d.ts
@@ -28,13 +28,15 @@ export type GlassTabBarProps = TabListProps & {
     theme?: Partial<GlassTabBarTheme>;
     /** Haptic tick while the scrub crosses tab boundaries (iOS). */
     haptics?: boolean;
+    /** Appearance for the bottom progressive fade (defaults to system). */
+    edgeFadeScheme?: 'light' | 'dark';
 };
 /**
  * Floating liquid-glass tab bar with Revolut-style minimize-on-scroll,
  * a sliding highlight, and finger scrubbing. Use via `TabList asChild`
  * with expo-router's headless tabs.
  */
-export declare function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics, ...props }: GlassTabBarProps): import("react").JSX.Element;
+export declare function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics, edgeFadeScheme, ...props }: GlassTabBarProps): import("react").JSX.Element;
 /** One tab trigger: icon + label that fades when minimized. */
 export declare function GlassTabButton({ item, index, isFocused, onPress, ...props }: TabTriggerSlotProps & {
     item: GlassTabItem;
diff --git a/node_modules/expo-glass-tabs/build/glass-tab-bar.js b/node_modules/expo-glass-tabs/build/glass-tab-bar.js
index 420cb04..061f4a7 100644
--- a/node_modules/expo-glass-tabs/build/glass-tab-bar.js
+++ b/node_modules/expo-glass-tabs/build/glass-tab-bar.js
@@ -49,7 +49,7 @@ const BarContext = createContext(null);
  * a sliding highlight, and finger scrubbing. Use via `TabList asChild`
  * with expo-router's headless tabs.
  */
-export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics = true, ...props }) {
+export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics = true, edgeFadeScheme, ...props }) {
     const insets = useSafeAreaInsets();
     const { width: windowWidth } = useWindowDimensions();
     const minimized = useMinimizeState();
@@ -160,7 +160,7 @@ export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides,
     });
     const bottomOffset = Math.max(insets.bottom - 16, 12);
     const barContext = useMemo(() => ({ slideIndex, isDragging, theme }), [slideIndex, isDragging, theme]);
-    return (_jsxs(View, { ...props, pointerEvents: "box-none", style: { position: 'absolute', left: 0, right: 0, bottom: 0 }, children: [_jsx(ProgressiveBlur, { direction: "bottom", style: {
+    return (_jsxs(View, { ...props, pointerEvents: "box-none", style: { position: 'absolute', left: 0, right: 0, bottom: 0 }, children: [_jsx(ProgressiveBlur, { direction: "bottom", scheme: edgeFadeScheme, style: {
                     position: 'absolute',
                     left: 0,
                     right: 0,
diff --git a/node_modules/expo-glass-tabs/build/progressive-blur.js b/node_modules/expo-glass-tabs/build/progressive-blur.js
index 50889ae..5306b71 100644
--- a/node_modules/expo-glass-tabs/build/progressive-blur.js
+++ b/node_modules/expo-glass-tabs/build/progressive-blur.js
@@ -1,22 +1,25 @@
 import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
 import { BlurView } from 'expo-blur';
-import { View } from 'react-native';
+import { useColorScheme, View } from 'react-native';
 /**
- * Progressive (gradient) blur: strongest at the anchored edge, fading to
- * none. iOS has no public variable-blur API, so we stack many thin
- * BlurViews with a tiny per-layer intensity — each layer's edge adds only
- * an imperceptible step, so the falloff reads as continuous. A soft black
- * gradient smooths the tail and keeps overlaid content legible.
+ * Progressive (gradient) blur — scheme-aware so light mode is not muddied
+ * by a hardcoded dark veil.
  */
-export function ProgressiveBlur({ style, intensity = 5, direction = 'top', ...rest }) {
+export function ProgressiveBlur({ style, intensity = 5, direction = 'top', scheme: schemeProp, ...rest }) {
+    const systemScheme = useColorScheme();
+    const isDark = (schemeProp ?? systemScheme) === 'dark';
     const heights = ['100%', '88%', '76%', '64%', '54%', '44%', '36%', '28%', '22%', '16%'];
     const anchor = direction === 'top' ? { top: 0 } : { bottom: 0 };
-    return (_jsxs(View, { pointerEvents: "none", style: style, ...rest, children: [heights.map((height, index) => (_jsx(BlurView, { tint: "dark", intensity: intensity, style: { position: 'absolute', left: 0, right: 0, height, ...anchor } }, index))), _jsx(View, { style: {
+    const tint = isDark ? 'dark' : 'light';
+    const gradient = isDark
+        ? `linear-gradient(to ${direction === 'top' ? 'bottom' : 'top'}, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.22) 42%, rgba(0,0,0,0.06) 68%, rgba(0,0,0,0) 88%)`
+        : `linear-gradient(to ${direction === 'top' ? 'bottom' : 'top'}, rgba(247,247,247,0.72) 0%, rgba(247,247,247,0.28) 42%, rgba(247,247,247,0.08) 68%, rgba(247,247,247,0) 88%)`;
+    return (_jsxs(View, { pointerEvents: "none", style: style, ...rest, children: [heights.map((height, index) => (_jsx(BlurView, { tint: tint, intensity: isDark ? intensity : Math.max(intensity - 1, 2), style: { position: 'absolute', left: 0, right: 0, height, ...anchor } }, index))), _jsx(View, { style: {
                     position: 'absolute',
                     top: 0,
                     left: 0,
                     right: 0,
                     bottom: 0,
-                    experimental_backgroundImage: `linear-gradient(to ${direction === 'top' ? 'bottom' : 'top'}, rgba(0,0,0,0.70) 0%, rgba(0,0,0,0.32) 42%, rgba(0,0,0,0.08) 68%, rgba(0,0,0,0) 88%)`,
+                    experimental_backgroundImage: gradient,
                 } })] }));
 }
diff --git a/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx b/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx
index c0f798b..c801502 100644
--- a/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx
+++ b/node_modules/expo-glass-tabs/src/glass-tab-bar.tsx
@@ -100,6 +100,8 @@ export type GlassTabBarProps = TabListProps & {
   theme?: Partial<GlassTabBarTheme>;
   /** Haptic tick while the scrub crosses tab boundaries (iOS). */
   haptics?: boolean;
+  /** Appearance for the bottom progressive fade (defaults to system). */
+  edgeFadeScheme?: 'light' | 'dark';
 };
 
 /**
@@ -112,6 +114,7 @@ export function GlassTabBar({
   onIndexSelected,
   theme: themeOverrides,
   haptics = true,
+  edgeFadeScheme,
   ...props
 }: GlassTabBarProps) {
   const insets = useSafeAreaInsets();
@@ -272,6 +275,7 @@ export function GlassTabBar({
       {/* Progressive blur rising from the screen's bottom edge behind the pill. */}
       <ProgressiveBlur
         direction="bottom"
+        scheme={edgeFadeScheme}
         style={{
           position: 'absolute',
           left: 0,
diff --git a/node_modules/expo-glass-tabs/src/progressive-blur.tsx b/node_modules/expo-glass-tabs/src/progressive-blur.tsx
index a3355f5..aadea11 100644
--- a/node_modules/expo-glass-tabs/src/progressive-blur.tsx
+++ b/node_modules/expo-glass-tabs/src/progressive-blur.tsx
@@ -1,30 +1,44 @@
 import { BlurView } from 'expo-blur';
-import { View, ViewProps } from 'react-native';
+import { useColorScheme, View, ViewProps } from 'react-native';
 
 type Props = ViewProps & {
   intensity?: number;
   /** Which edge the blur is anchored to (strongest there, fading away). */
   direction?: 'top' | 'bottom';
+  /** Prefer app theme over system appearance when provided. */
+  scheme?: 'light' | 'dark';
 };
 
 /**
  * Progressive (gradient) blur: strongest at the anchored edge, fading to
  * none. iOS has no public variable-blur API, so we stack many thin
  * BlurViews with a tiny per-layer intensity — each layer's edge adds only
- * an imperceptible step, so the falloff reads as continuous. A soft black
- * gradient smooths the tail and keeps overlaid content legible.
+ * an imperceptible step, so the falloff reads as continuous. A soft
+ * scheme-matched gradient smooths the tail and keeps overlaid content legible.
  */
-export function ProgressiveBlur({ style, intensity = 5, direction = 'top', ...rest }: Props) {
+export function ProgressiveBlur({
+  style,
+  intensity = 5,
+  direction = 'top',
+  scheme: schemeProp,
+  ...rest
+}: Props) {
+  const systemScheme = useColorScheme();
+  const isDark = (schemeProp ?? systemScheme) === 'dark';
   const heights = ['100%', '88%', '76%', '64%', '54%', '44%', '36%', '28%', '22%', '16%'] as const;
   const anchor = direction === 'top' ? { top: 0 } : { bottom: 0 };
+  const tint = isDark ? 'dark' : 'light';
+  const gradient = isDark
+    ? `linear-gradient(to ${direction === 'top' ? 'bottom' : 'top'}, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.22) 42%, rgba(0,0,0,0.06) 68%, rgba(0,0,0,0) 88%)`
+    : `linear-gradient(to ${direction === 'top' ? 'bottom' : 'top'}, rgba(247,247,247,0.72) 0%, rgba(247,247,247,0.28) 42%, rgba(247,247,247,0.08) 68%, rgba(247,247,247,0) 88%)`;
 
   return (
     <View pointerEvents="none" style={style} {...rest}>
       {heights.map((height, index) => (
         <BlurView
           key={index}
-          tint="dark"
-          intensity={intensity}
+          tint={tint}
+          intensity={isDark ? intensity : Math.max(intensity - 1, 2)}
           style={{ position: 'absolute', left: 0, right: 0, height, ...anchor }}
         />
       ))}
@@ -35,7 +49,7 @@ export function ProgressiveBlur({ style, intensity = 5, direction = 'top', ...re
           left: 0,
           right: 0,
           bottom: 0,
-          experimental_backgroundImage: `linear-gradient(to ${direction === 'top' ? 'bottom' : 'top'}, rgba(0,0,0,0.70) 0%, rgba(0,0,0,0.32) 42%, rgba(0,0,0,0.08) 68%, rgba(0,0,0,0) 88%)`,
+          experimental_backgroundImage: gradient,
         }}
       />
     </View>
